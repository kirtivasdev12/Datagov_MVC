@model Datagov_MVC.Models.Parks

@{
    ViewData["Title"] = "Home Page";
}


<h2>Index</h2>

@foreach (Datagov_MVC.Models.Park park in Model.data)
{
    @(park.parkCode + " " + park.fullName + " " + park.latLong)
    <br />
}

